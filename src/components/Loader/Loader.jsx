import React from 'react';

const Loader = (props) => {
  return (
      <>
        <div className="loader">
            <h4 className="loader">Loading...</h4>
        </div>
      </>
  );
};

export default Loader;